package com.exo1;

public interface EstComparable {
    /**
     * Compare 2 objects
     * @param obj the second objects to compare
     * @return -1 if x < y; 0 if x == y; 1 if x > y
     */
    int compareA(Object obj);
}
